/* eslint-disable no-unused-vars */
export enum Scenes {
	None = "None",
	Scene01 = "Scene01",
}

export enum Controls {
	UP = "UP",
	DOWN = "DOWN",
	LEFT = "LEFT",
	RIGHT = "RIGHT",
	SPACE = "SPACE",
}

export enum KeyMapErrorCode {
	NO_ERRORS = 0,
	NO_ACTION_FOUND = 1,
	KEY_ALREADY_MAPPED = 2,
}

export enum Players {
	White = "white",
	Black = "black"
}